package backend.accounts.users.instances;

import backend.accounts.users.framework.User;

public class Student extends User {
    //TODO to be implemented with customised features (e.g. apply 20% discount to every price in the app)
}
